# ===========================================================================
# Profile (at-a-glance):
#   Model:     Gemma-4-12B-it (bf16 — unsloth ungated mirror of google/gemma-4-12B-it)
#   Engine:    vLLM (vllm/vllm-openai:gemma4-unified — Gemma-4 arch-preview image)
#   Topology:  Dual 3090 PCIe (TP=2, no NVLink → custom all-reduce auto-disabled)
#   Drafter:   assistant speculative-decode, n=4 (google/gemma-4-12B-it-assistant ~0.5B)
#   KV:        bf16 / auto — single kv-dtype (vLLM); no FP8/INT8-PTH on this image
#   Vision:    yes (gemma4_unified is multimodal; served text-only here)
#   Max ctx:   256K (262144) via local p-RoPE cache overlay. The HF config's trained
#              max_position_embeddings is 131072, but the model card advertises 256K via
#              proportional RoPE. Stock gemma4_unified OOB-crashes past 131072 because its
#              RoPE cache is sized from config.max_position_embeddings; this compose mounts
#              vllm-gemma4-prope-longctx so the fixed cache is sized from runtime max_model_len.
#              Drop/re-test when vllm#39914 lands upstream.
#   Genesis:   N/A — Genesis is Qwen3-Next-specific
#   Status:    🧪 Experimental — arch-preview image; sm_86 support UNVERIFIED
#   Best for:  the target config — dual-3090 Gemma-4-12B + assistant spec-dec (vLLM's recipe, Ampere-adapted)
# ---------------------------------------------------------------------------
# base.yml (no-draft sibling) + the assistant speculative-decode draft, mirroring
# the gemma-4-31b dual's --speculative-config. Ampere form: TP=2 + the
# detect_nvlink.sh entrypoint that injects --disable-custom-all-reduce (PCIe,
# no NVLink), bf16, gemma4 parsers.
#
# Validate base.yml first (the sm_86-kernel bring-up test). The draft model
# `gemma-4-12B-it-assistant` is UNGATED (unlike the base) — fetch it ungated:
#   hf download google/gemma-4-12B-it-assistant --local-dir $MODEL_DIR/gemma-4-12b-it-assistant
# Default SPEC_MODEL below expects it at /root/.cache/huggingface/gemma-4-12b-it-assistant.
#
# Weights (base): hf download unsloth/gemma-4-12b-it --local-dir $MODEL_DIR/gemma-4-12b-it
# The arch-preview image tag may be ephemeral — pin a digest before shipping.

services:
  vllm-gemma-4-12b-dual-mtp:
    image: ${VLLM_IMAGE:-vllm/vllm-openai:gemma4-unified}
    container_name: "${ESTATE_CONTAINER:-vllm-gemma-4-12b-dual-mtp}"
    restart: ${CLUB3090_RESTART:-unless-stopped}
    ports:
      - "${ESTATE_PORT:-${PORT:-8036}}:8000"
    volumes:
      - ${MODEL_DIR:-../../../../../../models-cache}:/root/.cache/huggingface
      - ../../../cache/torch_compile:/root/.cache/vllm/torch_compile_cache
      - ../../../cache/triton:/root/.triton/cache
      - ../../../../../../scripts/detect_nvlink.sh:/etc/club3090/detect_nvlink.sh:ro
      - ../../../patches/vllm-gemma4-prope-longctx:/etc/club3090/gemma4-prope-longctx:ro
    environment:
      - NVIDIA_VISIBLE_DEVICES=${ESTATE_GPUS:-${NVIDIA_VISIBLE_DEVICES:-all}}
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - NVLINK_MODE=${NVLINK_MODE:-auto}
      - NCCL_CUMEM_ENABLE=0
      - NCCL_P2P_DISABLE=1
      - VLLM_NO_USAGE_STATS=1
      - OMP_NUM_THREADS=1
      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}
      - CUDA_LAUNCH_BLOCKING=${CUDA_LAUNCH_BLOCKING:-0}
      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
      - TRITON_CACHE_DIR=/root/.triton/cache
    shm_size: "16gb"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    entrypoint:
      - /bin/bash
      - -c
      - |
        bash /etc/club3090/gemma4-prope-longctx/install.sh
        # PCIe (no NVLink) → inject --disable-custom-all-reduce; NVLink rigs skip it.
        source /etc/club3090/detect_nvlink.sh
        if [ "${_NVLINK_ENABLED:-0}" = "1" ]; then
          exec vllm serve "$@"
        else
          exec vllm serve --disable-custom-all-reduce "$@"
        fi
      - --
    command:
      - --override-generation-config
      - '{"temperature":${TEMP:-${TEMPERATURE:-1.0}},"top_p":${TOP_P:-0.95},"top_k":${TOP_K:-64},"min_p":${MIN_P:-0.0},"repetition_penalty":${REPEAT_PENALTY:-1.0}}'
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --model
      - /root/.cache/huggingface/${MODEL_SUBDIR:-gemma-4-12b-it}
      - --served-model-name
      - ${SERVED_NAME:-gemma-4-12b}
      - --tensor-parallel-size
      - "${TP:-2}"
      - --pipeline-parallel-size
      - "${PP:-1}"
      - --max-model-len
      - "${MAX_MODEL_LEN:-${CTX:-262144}}"
      - --gpu-memory-utilization
      - "${GPU_MEMORY_UTILIZATION:-0.90}"
      - --max-num-seqs
      - "${MAX_NUM_SEQS:-4}"
      - --max-num-batched-tokens
      - "4096"
      - --trust-remote-code
      - --enable-auto-tool-choice
      - --tool-call-parser
      - gemma4
      - --reasoning-parser
      - gemma4
      - --chat-template
      - /vllm-workspace/examples/tool_chat_template_gemma4.jinja
      - --speculative-config
      - '{"model":"${SPEC_MODEL:-/root/.cache/huggingface/gemma-4-12b-it-assistant}","num_speculative_tokens":${SPEC_N_MAX:-4}}'

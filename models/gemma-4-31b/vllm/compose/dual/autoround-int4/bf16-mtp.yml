# ===========================================================================
# Profile (at-a-glance):
#   Model:     Gemma 4 31B (Intel AutoRound INT4)
#   Topology:  Dual 3090 (TP=2, NVLink auto-detected via NVLINK_MODE) — single-card boot-OOMs on 24 GB
#   Drafter:   MTP n=4 (Google's official `gemma-4-31B-it-assistant`, BF16 0.5B)
#   KV:        bfloat16 (2 bytes/token)
#   Vision:    yes
#   Max ctx:   131K default @ 4 streams (NIAH-validated to 120K) — BF16 KV pool
#              196,527 tok @ mem-util 0.95. For native 262K use vllm/gemma-int8-mtp.
#   Genesis:   N/A — Genesis is Qwen3-Next-specific
#   Status:    ✅ Production
#   Quality:   reasoning-ON 111/150 (74%) — det 65/75 · sandbox 46/75
#                TC 14/15 · IF 14/15 · SO 14/15 · DE 11/15 · RM 12/15 · BF 15/15 · HA 12/20 · CLI 19/40
#                (--full, --enable-thinking, --reasoning-parser gemma4, v0.22.0, 2026-05-31). Thinking-OFF
#                default NOT separately 8-packed this round (≈ int8-mtp's 105/150, KV near-lossless).
#                Supersedes the 2026-05-09 126/150 (looser v0.4 shape-check grader — not comparable).
#   Best for:  Gemma 4 dual-card STABLE long-ctx (stock v0.22.0 KV, 131K) — vision + tools.
#              The dual default is vllm/gemma-int8-mtp (native 262K); use this when you want
#              the stable bf16 KV path (no #40391 per-head-KV overlay) and 131K ctx is enough.
#              Carries ONE small overlay: the #42006 streaming multi-tool-call fix (both duals
#              share it — stock v0.22.0 drops non-last tool-call args when streaming).
# ---------------------------------------------------------------------------
# Gemma-4-31B-it (Intel AutoRound INT4) + Google MTP "assistant" drafter
# 2× RTX 3090 TP=2 + bfloat16 KV + 32K ctx + 4 streams.
#
# Status: COMMUNITY-CONTRIBUTED, EXPERIMENTAL.
#   First Ampere consumer cross-rig data on Google's Gemma 4 MTP drafters
#   (released 2026-05-05). Google benched on RTX PRO 6000 Blackwell;
#   this compose tests the same path on 2× RTX 3090 (Ampere sm_86, PCIe-only).
#
# Bench (2× 3090 PCIe, no NVLink, 230W cap, 2026-05-05 evening):
#   narrative (canonical 800-word essay): 109.16 wall TPS (CV 3.0%)
#   code (canonical quicksort):           140.59 wall TPS (CV 1.7%)
#   AL: 3.05 narr / 3.99 code (n=4 max — code essentially saturating)
#   Per-position accept narr: 79/57/41/29 %
#   Per-position accept code: 92/77/66/56 %
#   VRAM: 21.5 GB/card
#   Speedup vs no-spec-decode baseline (~61 TPS): 1.79× narr / 2.31× code.
#
# Models:
#   target: Intel/gemma-4-31B-it-int4-AutoRound  (21.2 GB, vision preserved)
#   draft : google/gemma-4-31B-it-assistant       (0.5B / 927 MB BF16)
#
# Pre-merge dependencies — both LANDED 2026-05-08:
#   1. vLLM PR #41745 — Gemma 4 MTP — MERGED 2026-05-06 (commit 27e0057a).
#      Nightly tag `1acd67a795...` (2026-05-08) is post-merge → overlay dropped.
#   2. transformers ≥ 5.8.0 — released 2026-05-05. Verified: post-merge nightly
#      ships transformers 5.8.0 → entrypoint upgrade line dropped.
# Pin uses the post-Gemma4-merge nightly. Note: Qwen3.6 composes stay on the
# v7.72.2 PROD pin (01d4d1ad3) since Genesis allowlist anchors there.
#
# KV format pinned in a corner on Ampere:
#   - fp8_e5m2 → assert fail in gemma4_mm.py:1336 (vLLM allowlist excludes it)
#   - fp8_e4m3 → Triton "fp8e4nv not supported in this architecture" on sm_86
#     (Ampere supports only fp8e4b15 / fp8e5; not fp8e4nv)
#   - default (auto = bfloat16) → sidesteps all fp8 kernel paths
# Smaller KV pool than fp8 but at 32K test ctx that's not the bottleneck.
# ===========================================================================
# Hardware metadata (parsed by scripts/preflight.sh):
# Requires-min-vram-gb: 24
# Engine-profile: vllm-gemma-stable
# Requires-min-gpu-count: 2
# Tensor-parallel: 2
services:
  vllm-gemma-4-31b-mtp:
    image: ${VLLM_IMAGE:-vllm/vllm-openai:v0.22.0}
    container_name: "${ESTATE_CONTAINER:-vllm-gemma-4-31b-mtp}"
    restart: ${CLUB3090_RESTART:-unless-stopped}
    ports:
      - "${ESTATE_PORT:-${PORT:-8030}}:8000"
    volumes:
      - ${MODEL_DIR:-../../../../../../models-cache}:/root/.cache/huggingface
      # torch.compile + Triton kernel caches — first boot warms (~5-7 min);
      # subsequent boots reuse cached graphs (~3 min). Pattern from dual-turbo.yml.
      - ../../../cache/torch_compile:/root/.cache/vllm/torch_compile_cache
      - ../../../cache/triton:/root/.triton/cache
      # PR #41745 overlay dropped 2026-05-08: merged upstream + nightly contains it.
      # vLLM PR #42006 — Gemma 4 MTP streaming multi-tool-call fix (lean diff-apply).
      # Stock v0.22.0 drops the args of all-but-the-last tool call in a STREAMED
      # multi-tool response (live-confirmed 2026-05-31). The ONLY overlay this
      # compose carries. Drop when #42006 merges. See the dir's README.
      - ../../../patches/vllm-pr42006-v0.22.0:/etc/club3090/pr42006:ro
      # NVLink auto-detection — runs inside container at boot.
      - ../../../../../../scripts/detect_nvlink.sh:/etc/club3090/detect_nvlink.sh:ro
    environment:
      - NVIDIA_VISIBLE_DEVICES=${ESTATE_GPUS:-${NVIDIA_VISIBLE_DEVICES:-all}}
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-${OFFLINE:-0}}
      - TRANSFORMERS_OFFLINE=${TRANSFORMERS_OFFLINE:-${OFFLINE:-0}}
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - NVLINK_MODE=${NVLINK_MODE:-auto}
      - NCCL_CUMEM_ENABLE=0
      - NCCL_P2P_DISABLE=1
      - VLLM_NO_USAGE_STATS=1
      - OMP_NUM_THREADS=1
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:512
      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
      - TRITON_CACHE_DIR=/root/.triton/cache
    shm_size: "16gb"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    # transformers 5.8.0+ ships in the post-Gemma4-merge nightly (verified
    # 2026-05-08: nightly-1acd67a795... has transformers 5.8.0). Entrypoint
    # upgrade dropped.
    entrypoint:
      - /bin/bash
      - -c
      - |
        # vLLM PR #42006 (Gemma 4 streaming multi-tool-call fix) — apply before import.
        bash /etc/club3090/pr42006/install.sh
        # NVLink auto-detection (sets NCCL env vars, _NVLINK_ENABLED).
        source /etc/club3090/detect_nvlink.sh
        if [ "${_NVLINK_ENABLED:-0}" = "1" ]; then
          exec vllm serve "$@"
        else
          exec vllm serve --disable-custom-all-reduce "$@"
        fi
      - --
    command:
      - --override-generation-config
      - '{"temperature":${TEMP:-${TEMPERATURE:-1.0}},"top_p":${TOP_P:-0.95},"top_k":${TOP_K:-64},"min_p":${MIN_P:-0.0},"repetition_penalty":${REPEAT_PENALTY:-1.0}}'
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --model
      - /root/.cache/huggingface/gemma-4-31b-autoround-int4
      - --served-model-name
      - gemma-4-31b-autoround
      - --tensor-parallel-size
      - "${TP:-2}"
      - --pipeline-parallel-size
      - "${PP:-1}"
      # max-model-len (TP=2, BF16 KV) — DUAL-CARD RULE: prioritize max context over
      # concurrency (see docs/DUAL_CARD.md). max-num-seqs is a CAP, not a reservation,
      # and the KV pool is fixed by VRAM, NOT by max-model-len — so raising the ceiling
      # is ~free for short-request concurrency; it only lets a single request go bigger.
      #
      # MEASURED (vLLM boot log, 2026-05-31, 2× 3090 @ 0.95): BF16 KV pool = 196,527 tok.
      # Concurrency is opportunistic against that shared pool:
      #   max-model-len 131072 @ max-num-seqs=4  → DEFAULT. 4-wide for short/medium
      #                          requests; a single request fillable to ~120K (NIAH-
      #                          validated: verify-stress ceiling ladder 94K + 120K clean).
      #   max-num-seqs=2         → guarantees 2 concurrent long-ctx agents un-preempted.
      #   max-num-seqs=1         → guarantees one long request never preempted.
      # The pool caps a SINGLE request at ~196K; we default the ceiling to 131072 (clean,
      # NIAH-validated, leaves ~65K pool headroom). fp8 KV blocked on Ampere (see header).
      #
      # ⚠️ Vision + near-max ctx: at ~120K single-stream only ~1.4 GB/card is free, so a
      #    large image AT high ctx can OOM. For vision-heavy long-ctx, drop max-num-seqs
      #    to 1-2 or GPU_MEMORY_UTILIZATION to 0.92. Vision at typical ctx is unaffected.
      # Override: MAX_MODEL_LEN=49152 MAX_NUM_SEQS=4 bash scripts/switch.sh vllm/gemma-bf16-mtp
      - --max-model-len
      - "${MAX_MODEL_LEN:-131072}"
      - --gpu-memory-utilization
      - "${GPU_MEMORY_UTILIZATION:-0.95}"
      - --max-num-seqs
      - "${MAX_NUM_SEQS:-4}"
      # Vision tower: max_tokens_per_mm_item=2496 must fit in batched tokens.
      - --max-num-batched-tokens
      - "4096"
      - --trust-remote-code
      # Tool-call support per vLLM Gemma 4 recipe — required for any soak /
      # agent traffic that includes a `tools: [...]` array. Without these,
      # vLLM rejects tool-bearing requests with HTTP 400.
      - --enable-auto-tool-choice
      - --tool-call-parser
      - gemma4
      # Route Gemma-4's <|channel>thought…<channel|> reasoning trace into
      # reasoning_content (vLLM ships Gemma4ReasoningParser). Without it,
      # enable_thinking responses leave the trace in `content` → breaks
      # JSON/structured output. Safe with thinking OFF (no-ops when the model
      # emits no channel tokens). Validated 2026-05-31 on int8 (StructOutput
      # 8→14, DataExtract 0→9 thinking-on; invalid_json eliminated).
      - --reasoning-parser
      - gemma4
      - --chat-template
      - /vllm-workspace/examples/tool_chat_template_gemma4.jinja
      - --speculative-config
      - '{"model":"/root/.cache/huggingface/gemma-4-31b-it-assistant","num_speculative_tokens":4}'
